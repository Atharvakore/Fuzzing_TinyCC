# Uncomment the following 2 lines to use the fuzzingbook
 fuzzingbook==1.2.2
 pyan 
 coverage
 z3-solver>=4.12.0.0 # the version specified in fuzzingbook cannot be built on Ubuntu 24.04 because of cmake version incompatibility, so we use a manually specified version